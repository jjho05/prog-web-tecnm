# 0.4 HTML Headings & Paragraphs: El Arte de la Tipografía

> 📰 **Objetivo de la Lección:** Aprender a estructurar contenido para humanos y robots (SEO), entender cómo el navegador trata el texto y usar etiquetas de formato semántico.

---

## 1. Headings: La Jerarquía de la Información

Imagina que estás escribiendo un libro. Tienes el Título del Libro, los Capítulos, y los Subtítulos dentro de los capítulos.
HTML funciona igual. Los buscadores (Google) no "leen" tu página como un humano, ellos "escanean" la estructura de títulos para saber qué es importante.

### Los 6 Niveles (H1 - H6)

*   **`<h1>` (El Rey):**
    *   Representa el **tema principal** de toda la página.
    *   **Regla de Oro:** Solo debe haber **UNO** por página.
    *   *Ejemplo:* "Curso de HTML5 desde Cero".
*   **`<h2>` (El Príncipe):**
    *   Representa las secciones principales.
    *   *Ejemplo:* "Módulo 1: Introducción", "Módulo 2: Sintaxis".
*   **`<h3>` (El Duque):**
    *   Sub-secciones dentro de un H2.
    *   *Ejemplo:* "Instalación del IDE" (dentro de Módulo 1).
*   **`<h4>` - `<h6>`:**
    *   Detalles cada vez más específicos. Rara vez pasarás del `<h4>`.

### El Error del Principiante ❌
*"Voy a usar `<h6>` porque quiero un título con letra chiquita".*
**¡NO!** El tamaño de letra se cambia con CSS (`font-size`). Las etiquetas `h1-h6` definen **significado**, no estética.

---

## 2. Paragraphs: El Flujo del Texto

La etiqueta `<p>` es el bloque básico de texto. Pero tiene comportamientos curiosos que debes conocer.

### Colapso de Espacios en Blanco (White Space Collapse)
El navegador es muy estricto con los espacios.

**Tu código:**
```html
<p>
    Hola               Mundo.
    ¿Cómo              están?
</p>
```

**Lo que ve el usuario:**
`Hola Mundo. ¿Cómo están?`

HTML ignora los enter (saltos de línea) y convierte múltiples espacios en **uno solo**.

### ¿Cómo fuerzo un salto de línea? (`<br>`)
Si estás escribiendo una dirección postal o un poema, necesitas saltos forzados.
Usa la etiqueta `<br>` (Line Break).

```html
<p>
  Calle Falsa 123<br>
  Ciudad de México<br>
  CP 06000
</p>
```
*Nota: No abuses del `<br>` para crear espacio entre párrafos. Para eso se usa CSS (`margin`).*

### Texto Preformateado (`<pre>`)
Si REALMENTE necesitas que se respeten tus espacios (como al mostrar código de programación o arte ASCII), usa `<pre>`.

```html
<pre>
    |￣￣￣￣￣￣|
    |  HOLA    |
    |__________;
      (__/) ||
      (•ㅅ•) ||
      / 　 づ
</pre>
```

---

## 3. Semántica de Texto: Más allá de la Negrita

En los viejos tiempos, usábamos `<b>` (Bold) para negrita y `<i>` (Italic) para cursiva.
Hoy usamos etiquetas que dan **significado**:

| Etiqueta Vieja (Visual) | etiqueta Nueva (Semántica) | Significado para el Robot/Lector |
| :--- | :--- | :--- |
| `<b>` | `<strong>` | "Esto es texto de **FUERTE IMPORTANCIA**". El lector de voz cambiará el tono. |
| `<i>` | `<em>` | "Esto tiene *énfasis*". Como cuando cambias la entonación al hablar. |
| `<s>` (Strikethrough) | `<del>` | "Este texto ha sido **eliminado** o corregido" (útil en listas de tareas). |
| `<u>` (Underline) | `<ins>` | "Este texto ha sido **insertado** o añadido". |

### Otras Etiquetas de Formato Útiles
*   `<mark>`: Resalta el texto (como un marcatextos amarillo). Útil para resultados de búsqueda.
*   `<small>`: Texto legal o comentarios al margen. Reduce el tamaño semánticamente.
*   `<sub>` y `<sup>`: Subíndices y Superíndices (H<sub>2</sub>O, E=mc<sup>2</sup>).
*   `<blockquote>`: Para citas textuales largas. El navegador suele ponerle márgenes.
*   `<q>`: Para citas cortas (pone comillas automáticamente).

---

## 4. La línea Horizontal (`<hr>`)
### 4. Citas y Referencias (Quotes)
HTML tiene etiquetas semánticas para decir "Esto no lo dije yo, lo dijo otro".
*   `<blockquote>`: Para citar un bloque grande (tiene sangría automática).
*   `<q>`: Para citas cortas en medio de una frase (pone comillas automáticas).

> 🎓 **Abre este archivo:** `codigos/04_04_citas.html`
> Aprende a dar crédito a quien lo merece.

```html
<!-- Cita en Bloque (Párrafo entero) -->
<blockquote>
    "El conocimiento es poder."
</blockquote>

<!-- Cita en Línea (Frase pequeña) -->
<p>
    Como dijo Einstein: <q>La imaginación es más importante que el conocimiento.</q>
</p>
```

### 5. Caracteres Especiales (Entities)
¿Cómo escribes `<` sin que el navegador crea que es una etiqueta?
Usando entidades. Empiezan con `&` y terminan con `;`.
*   `&lt;` = `<` (Less Than)
*   `&gt;` = `>` (Greater Than)
*   `&copy;` = © (Copyright)

> 🎓 **Abre este archivo:** `codigos/04_05_entidades.html`
> Una lista útil para cuando el teclado no basta.

```html
<!-- Símbolos que rompen HTML -->
<p> 5 &lt; 10  (Menor que) </p>
<p> 20 &gt; 5  (Mayor que) </p>

<!-- Símbolos de Copyright -->
<p> &copy; 2026 Mi Empresa </p>
```

### 6. Contacto (`<address>`)
No uses un simple `<p>` para poner tu dirección o email. Usa `<address>`.
Le dice a Google que esa es la información de contacto oficial.

> 🎓 **Abre este archivo:** `codigos/04_06_direccion.html`

```html
<address>
    Escrito por: <a href="mailto:yo@email.com">Jesús</a>.<br>
    Visítanos en: Av. Tecnológico #123
</address>
```

---

## 7. La línea Horizontal (`<hr>`)

La etiqueta `<hr>` (Horizontal Rule) dibuja una línea divisoria a través de la página.
Úsala para separar temas grandes (un cambio temático en el texto), no solo para decorar.

---

## 8. Práctica Sugerida

Crea un archivo `poema.html` e intenta reproducir esto:

1.  Usa un `<h1>` para el título del poema.
2.  Usa un `<h3>` para el autor.
3.  Escribe el poema usando `<p>` y `<br>` para las estrofas.
4.  Usa `<strong>` para resaltar palabras clave.
5.  Usa `<hr>` al final.
6.  Agrega un pie de página (`<small>`) con el año.

> 📂 **Códigos de Ejemplo:**
> *   `codigos/04_01_titulos.html` (Jerarquía H1-H6)
> *   `codigos/04_02_formato.html` (Negrita, Cursiva, Resaltado)
> *   `codigos/04_03_espacios.html` (El problema del espacio y `<pre>`)
> *   `codigos/04_04_citas.html` (Blockquote vs Q)
> *   `codigos/04_05_entidades.html` (Símbolos especiales `&copy;`)
> *   `codigos/04_06_direccion.html` (Etiqueta Address)

<div align="center">

⬅️ [Anterior: 0.3 Elements](0.3_html_elements_attributes.md) &nbsp;&nbsp;|&nbsp;&nbsp; [Siguiente: 0.5 Styles](0.5_html_styles_colors.md) ➡️

</div>
