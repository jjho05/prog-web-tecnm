# 0.2 HTML Basic: Anatomía de una Web

> 🌍 **Objetivo de la Lección:** Comprender la estructura fundamental de un documento HTML5, el propósito de las etiquetas semánticas básicas y cómo el navegador interpreta nuestro código.

---

## 1. ¿Qué es HTML realmente?

**HTML** significa *HyperText Markup Language* (Lenguaje de Marcado de Hipertexto).
*   **Hipertexto:** Texto que contiene enlaces a otros textos (la base de la web).
*   **Lenguaje de Marcado:** No es un lenguaje de programación (no tiene lógica, variables o funciones matemáticas). Es un lenguaje para **estructurar** contenido. Usamos "marcas" (etiquetas) para decirle al navegador: "Esto es un título", "Esto es una imagen", "Esto es un párrafo".

### Historia Rápida
Fue creado por **Tim Berners-Lee** en 1991 en el CERN (Suiza). Su objetivo original era simplemente compartir documentos científicos entre universidades. Hoy, impulsa desde blogs personales hasta aplicaciones como Netflix.

---

## 2. La Estructura "Boilerplate" (El Esqueleto)

Toda página web moderna DEBE tener la siguiente estructura mínima. Si falta algo, el navegador intentará adivinar, y a veces adivina mal.

```html
<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Primera Web</title>
</head>

<body>
    <h1>Hola Mundo</h1>
    <p>Este es el contenido visible.</p>
</body>

</html>
```

Vamos a analizarlo línea por línea. Esto es pregunta de examen y de entrevista laboral.

### A. `<!DOCTYPE html>`
No es una etiqueta HTML. Es una **instrucción al navegador**.
*   Le dice: "Hey, Chrome/Firefox, este documento sigue las reglas de **HTML5**".
*   Sin esto, los navegadores entran en **"Quirks Mode"** (Modo de Compatibilidad), simulando ser navegadores viejos de los 90s, lo que puede romper tu diseño.

### B. `html` (El Raíz)
Es el elemento padre de todos. Todo tu código debe vivir dentro de `<html>` y `</html>`.
*   El atributo `lang="es"` es vital para la accesibilidad y el SEO. Le dice a Google "Esta página está en Español", ayudando a que salga en búsquedas correctas y a que los lectores de pantalla usen el acento correcto.

---

## 3. `<head>` vs `<body>`: Los dos mundos

Una página web se divide en dos grandes áreas. Entender la diferencia es crucial.

### La Cabeza (`<head>`) 🧠
Contiene **Metadatos** (información sobre la información).
**NADA de lo que pongas aquí se dibuja en la página blanca del navegador.**

Aquí van:
1.  **`<meta charset="UTF-8">`**: La codificación de caracteres. Permite usar ñ, tildes (á, é, í), emojis (😎) y símbolos raros. Si lo olvidas, verás símbolos extraños como ``.
2.  **`<title>`**: El texto que aparece en la **pestaña** del navegador y en los resultados de Google.
3.  **`<meta name="viewport"...>`**: Crucial para celulares. Le dice al móvil que use el ancho real de la pantalla y no intente "alejar" la vista como si fuera un monitor de escritorio.
4.  Enlaces a archivos CSS (Estilos) y Favicons (el iconito de la pestaña).

### 4. El Cerebro: `<head>` y Metadatos
Todo lo que está dentro del `head` **no se ve** en la página, pero es vital para que Google te encuentre y para que el navegador sepa qué hacer.

### A. Meta Tags (Lo Invisible)
Son etiquetas que dan instrucciones al navegador.
*   **Description:** El texto que sale en Google cuando te buscan.
*   **Viewport:** OBLIGATORIO para que tu web se vea bien en celulares.

> 🎓 **Abre este archivo:** `codigos/02_04_meta_tags.html`
> Examina cómo funcionan estas etiquetas invisibles.

```html
<!-- EJEMPLO: META TAGS -->
<head>
    <meta charset="UTF-8">
    <!-- Descripción para Google -->
    <meta name="description" content="Tutorial de HTML5">
    <!-- Vital para celulares -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meta Tags</title>
</head>
```

### B. El Problema de la Ñ (Charset)
¿Alguna vez has visto símbolos raros como `Ã±` en lugar de `ñ`? Eso es un error de codificación.
Siempre, SIEMPRE usa `<meta charset="UTF-8">`.

> 🎓 **Abre este archivo:** `codigos/02_05_charset.html`
> Intenta borrar la línea del charset y mira cómo se rompen los acentos.

```html
<!-- EJEMPLO: EL PROBLEMA DEMOSTRADO -->
<head>
    <!-- Si borras esto, la 'Ñ' se verá como 'Ã±' -->
    <meta charset="UTF-8"> 
</head>
<body>
    <h1>Ñandú, Cigüeña, Camión</h1>
</body>
```

## 5. El Cuerpo: `<body>`) 🦴
Contiene el **Contenido Semántico**.
**TODO lo que quieras que el usuario vea va aquí.**
Textos, imágenes, videos, botones, menús, etc.

---

## 4. Sintaxis de las Etiquetas (Tags)

El lenguaje se basa en etiquetas. La mayoría funcionan en pares:

```html
<etiqueta atributo="valor"> Contenido </etiqueta>
```

1.  **Apertura:** `<p>` (Se "prende" el significado).
2.  **Contenido:** El texto o elementos dentro.
3.  **Cierre:** `</p>` (Tiene una barra `/`. Se "apaga" el significado).

### Elementos Vacíos (Void Elements)
Algunos elementos no tienen contenido, por lo tanto **no se cierran**.
*   `<img>`: Pone una imagen. No puedes poner texto "dentro" de una imagen.
*   `<br>`: Un salto de línea.
*   `<hr>`: Una línea horizontal separadora.
*   `<input>`: Un campo de texto para formularios.

*Nota: En versiones viejas (XHTML) se obligaba a cerrarlas así `<br />`, pero en HTML5 no es necesario.*

---

## 5. El Nivel de Títulos (SEO)

HTML ofrece 6 niveles de títulos: `<h1>` hasta `<h6>`.
No los elijas por el tamaño de la letra (eso se cambia con CSS). Elígelos por **importancia**.

*   **`<h1>` (Título Principal):** Debe haber **solo uno** por página. Describe de qué trata todo el documento. Es lo más importante para Google.
*   **`<h2>` (Secciones):** Títulos de capítulos importantes.
*   **`<h3>` (Sub-secciones):** Detalles dentro de los capítulos.

```html
<h1>Historia de México</h1>    <!-- El tema de la página -->
  <h2>Época Prehispánica</h2>  <!-- Una gran sección -->
    <h3>Los Mayas</h3>         <!-- Un subtema -->
    <h3>Los Aztecas</h3>       <!-- Otro subtema -->
  <h2>Época Colonial</h2>      <!-- Otra gran sección -->
```

---

## 6. Tu Tarea: El "Hola Mundo" Perfecto

1.  En tu carpeta `curso-web-master`, asegúrate de tener `index.html`.
2.  Escribe `!` y presiona `Tab`. Antigravity creará la estructura base por ti.
3.  Cambia el `lang` a `"es"`.
4.  Dentro del `body`, crea una estructura con:
    *   Un `h1` con tu nombre.
    *   Un `h2` que diga "Mis Objetivos".
    *   Un `p` explicando por qué quieres aprender a programar.
5.  Usa **Live Server** para verlo.

> 📂 **Códigos de Ejemplo:**
> *   `codigos/02_01_estructura_base.html` (La plantilla perfecta)
> *   `codigos/02_02_variante_minima.html` (Lo que NO debes hacer)
> *   `codigos/02_03_comentarios.html` (Mensajes ocultos)
> *   `codigos/02_04_meta_tags.html` (SEO y configuraciones ocultas)
> *   `codigos/02_05_charset.html` (El problema de los acentos/UTF-8)

<div align="center">

⬅️ [Anterior: 0.1 Editors](0.1_html_editors.md) &nbsp;&nbsp;|&nbsp;&nbsp; [Siguiente: 0.3 Elements](0.3_html_elements_attributes.md) ➡️

</div>
