# 0.7 HTML JavaScript: Despertando a la Bestia

> 🧠 **Objetivo de la Lección:** Entender la tríada de la web (HTML, CSS, JS), introducir la etiqueta `<script>`, los eventos de usuario y la manipulación básica del DOM.

---

## 1. La Tríada Sagrada

Imagina que estás construyendo una casa (o un cuerpo humano):

1.  **HTML (Estructura / Huesos):** Las paredes, vigas, puertas. Define *qué existe*.
2.  **CSS (Presentación / Piel y Ropa):** Pintura, decoración, alfombras. Define *cómo se ve*.
3.  **JavaScript (Comportamiento / Cerebro y Músculos):** Las luces que prenden al tocar un switch, la puerta automática del garaje. Define *qué hace*.

HTML y CSS son lenguajes estáticos. **JavaScript es un lenguaje de programación real.**
Con JS puedes hacer calculadoras, juegos (como el dinosaurio de Chrome), chats, mapas interactivos, etc.

---

## 2. La Etiqueta `<script>`

Para poner cerebro en tu HTML, usas la etiqueta `<script>`.
Puedes ponerla en el `<head>` o al final del `<body>`.
*Recomendación Pro:* Ponlo justo antes de cerrar el `</body>`. Así la página carga visualmente primero y luego carga la lógica.

```html
<body>
    <h1>Hola</h1>
    
    <!-- Aquí empieza la magia -->
    <script>
        alert("¡Bienvenido a mi web!");
    </script>
</body>
```

### Funciones Básicas para empezar
*   `alert("Mensaje")`: Lanza una ventana emergente molesta.
*   `console.log("Mensaje")`: **La herramienta #1 del programador.** Escribe mensajes en la consola "secreta" del navegador (F12 > Console). No molesta al usuario.

---

## 3. Eventos: Causa y Efecto ⚡

JS se queda dormido hasta que algo pasa. Esos "algos" se llaman **Eventos**.
Los ponemos como atributos en las etiquetas HTML.

*   `onclick`: Cuando el usuario da click.
*   `onmouseover`: Cuando el mouse entra al elemento.
*   `onmouseout`: Cuando el mouse sale.
*   `onkeydown`: Cuando presionas una tecla.
*   `onload`: Cuando la página terminó de cargar.

```html
<button onclick="alert('¡Auch!')">Goléame</button>

<img src="perro.jpg" onmouseover="alert('¡Guau!')">
```

---

## 4. El DOM (Document Object Model) 🌳

Aquí es donde las cosas se ponen serias.
El **DOM** es el árbol genealógico de tu página. JS ve tu página como un árbol de objetos.

JS tiene un poder: **Puede buscar cualquier etiqueta y cambiarla en vivo.**

### La Fórmula Sagrada:
`document.getElementById("ID-DEL-ELEMENTO").propiedad = "NUEVO VALOR"`

1.  **`document`**: Todo el archivo.
2.  **`getElementById`**: "Búscame la etiqueta que tenga este ID específico".
3.  **`.innerHTML`**: Lo que tiene adentro (texto o HTML).
4.  **`.style.color`**: Su estilo CSS.
5.  **`.src`**: Su atributo fuente (para imágenes).

### Ejemplo Completo: El Interruptor de Luz 💡

Imagina un foco (imagen) que cambia cuando le das click a un botón.

**Paso 1: HTML con IDs**
```html
<img id="foco" src="foco_apagado.png" width="100">
<p id="estado">Apagado</p>
```

**Paso 2: Botones con JS**
```html
<button onclick="document.getElementById('foco').src = 'foco_prendido.png'; document.getElementById('estado').innerHTML = 'Prendido'">
    Prender
</button>

<button onclick="document.getElementById('foco').src = 'foco_apagado.png'; document.getElementById('estado').innerHTML = 'Apagado'">
    Apagar
</button>
```

---

## 5. El Atributo `<noscript>`

Si un usuario paranoico desactiva JavaScript en su navegador, tu página podría romperse.
Usa `<noscript>` para mostrar un mensaje alternativo.

```html
<noscript>
    <p>⚠️ Oye, activa JavaScript o esta página no funcionará.</p>
</noscript>
```

---

## 6. Práctica Final de la Unidad 0 🎓

¡Felicidades! Has terminado el "Crash Course".
Ahora sabes usar herramientas, estructurar, escribir, diseñar y programar (básicamente).

**Tu Misión Final:**
Crea un archivo `proyecto_final.html`.
1.  Debe tener un título `<h1>` centrado con una fuente moderna.
2.  Una imagen tuya o de un avatar.
3.  Un botón que diga "Modo Oscuro".
4.  Al darle click al botón, el fondo de la página debe volverse negro y el texto blanco (Usando JS: `document.body.style...`).

> 📂 **Códigos de Ejemplo:**
> *   `codigos/07_01_alertas.html` (Hola Mundo JS)
> *   `codigos/07_02_dom.html` (Cambiando texto en vivo)
> *   `codigos/07_03_estilos.html` (Controlando CSS con botones)
> *   `codigos/07_04_proyecto_final.html` (Ejemplo completo Dark Mode)
> 🎓 **Abre este archivo:** `codigos/07_05_eventos_mouse.html`

```html
<!-- Evento OnClick -->
<button onclick="alert('¡Auch!')">Golpéame</button>

<!-- Evento OnMouseOver -->
<img src="perro.jpg" onmouseover="alert('¡Guau!')">
```
> 🎓 **Abre este archivo:** `codigos/07_06_inputs.html`

```javascript
function saludar() {
    // Leer lo que escribió el usuario
    var nombre = document.getElementById("cajaNombre").value;
    alert("Hola " + nombre);
}
```

> 🎓 **Abre este archivo:** `codigos/07_08_calculadora.html`

```javascript
// Sumar dos números (Cuidado con los textos)
var a = parseInt(document.getElementById("num1").value);
var b = parseInt(document.getElementById("num2").value);
var suma = a + b;
```

<div align="center">

⬅️ [Anterior: 0.6 Multimedia](0.6_html_images_links.md) &nbsp;&nbsp;|&nbsp;&nbsp; [Siguiente: Unidad 1.1](..//unidad1/1.1.md) ➡️

</div>
