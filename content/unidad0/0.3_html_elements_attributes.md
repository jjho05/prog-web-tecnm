# 0.3 HTML Elements & Attributes: Los Ladrillos de la Web

> 🧱 **Objetivo de la Lección:** Dominar la anatomía de una etiqueta, entender la diferencia crítica entre elementos de Bloque y de Línea, y aprender a usar atributos globales.

---

## 1. Anatomía de un Elemento HTML

No es solo poner `<p>`. Un elemento tiene partes específicas que debes conocer por nombre.

```
       Atributo   Valor
          ⬇️       ⬇️
   <p    class = "texto-rojo">    Hola Mundo    </p>
   ⬆️                         ⬆️              ⬆️
 Etiqueta                   Contenido      Etiqueta
 Apertura                                   Cierre
```

1.  **Opening Tag (Apertura):** Donde "nace" el elemento. Puede tener configuraciones (atributos).
2.  **Content (Contenido):** Lo que vive dentro. Puede ser texto u **otros elementos** (anidamiento).
3.  **Closing Tag (Cierre):** Donde "muere" el elemento. ¡Si la olvidas, el navegador puede confundirse y romper todo tu diseño!

---

## 2. Bloque vs Línea (Block vs Inline) ⚠️ *Concepto Vital*

Este es el concepto más importante para entender por qué tu web se ve como se ve.
Cada elemento HTML nace con un comportamiento por defecto:

### A. Elementos de Bloque (Block-level)
Son egoístas.
*   **Comportamiento:** Ocupan **todo el ancho disponible** (100% de la pantalla).
*   **Efecto:** Siempre empiezan en una **Línea Nueva**. Empujan todo lo demás hacia abajo.
*   **Ejemplos:**
    *   `<h1>` a `<h6>` (Títulos)
    *   `<p>` (Párrafos)
    *   `<div>` (Contenedores genéricos)
    *   `<ul>`, `<li>` (Listas)

```html
<!-- Estos dos elementos NUNCA estarán en la misma línea visual -->
<h1>Soy un bloque</h1>
<p>Yo también soy un bloque</p>
```

### B. Elementos de Línea (Inline)
Son sociables.
*   **Comportamiento:** Ocupan solo el **ancho necesario** para su contenido.
*   **Efecto:** Se quedan en la **misma línea** si hay espacio. No fuerzan saltos de línea.
*   **Ejemplos:**
    *   `<a>` (Enlaces)
    *   `<img>` (Imágenes)
    *   `<span>` (Contenedores de texto pequeños)
    *   `<b>`, `<i>` (Formato de texto)

```html
<!-- Estos elementos sí pueden convivir juntos -->
<p>
    Hola, soy un texto con una <a href="#">palabra linkeada</a> y una <b>negrita</b>.
</p>
```

---

## 3. Elementos Anidados (Nesting)

Puedes poner cajas dentro de cajas. Pero hay reglas de oro:
1.  **Cierra en orden inverso:** "El último en entrar es el primero en salir" (LIFO).
    *   ✅ `<p>Esto es <b>Negrita</b></p>`
    *   ❌ `<p>Esto es <b>Negrita</p></b>` (¡Crimen visual!)

2.  **Bloque come Línea:** Un elemento de Bloque puede contener elementos de Línea.
    *   ✅ `<div> <p>Texto</p> </div>`
3.  **Línea NO come Bloque:** Un elemento de línea (usualmente) no debe contener uno de bloque.
    *   ❌ `<a href="#"> <div>Click</div> </a>` (Aunque HTML5 lo permite en casos raros, es mala práctica para aprendices).

---

### C. Listas (Organizando el caos)
No uses guiones manuales. Usa las etiquetas correctas:
1.  **UL (Unordered List):** Puntos (Bullets). Para cosas sin orden (lista de compras).
2.  **OL (Ordered List):** Números (1, 2, 3). Para pasos o instrucciones.
3.  **DL (Definition List):** Diccionarios (Término y Definición).

> 🎓 **Abre estos archivos:**
> *   `codigos/03_03_listas_desordenadas.html`
> *   `codigos/03_04_listas_ordenadas.html`

```html
<!-- UL: Puntos (Desordenada) -->
<ul>
    <li>Leche</li>
    <li>Pan</li>
</ul>

<!-- OL: Números (Ordenada) -->
<ol>
    <li>Paso 1: Abrir</li>
    <li>Paso 2: Cerrar</li>
</ol>
```

### D. Tablas (Datos Estructurados)
¡OJO! Las tablas son para **datos** (como Excel), NO para diseñar la página (eso es de los 90s).
*   `<table>`: El contenedor.
*   `<tr>`: Table Row (Fila).
*   `<th>`: Table Header (Encabezado negrita).
*   `<td>`: Table Data (Dato normal).

> 🎓 **Abre este archivo:** `codigos/03_06_tablas.html`
> Mira cómo se construye una rejilla fila por fila.

```html
<!-- Estructura de Tabla -->
<table border="1">
  <tr> <!-- Fila 1 (Headers) -->
    <th>Nombre</th>
    <th>Edad</th>
  </tr>
  <tr> <!-- Fila 2 (Datos) -->
    <td>Jesús</td>
    <td>25</td>
  </tr>
</table>
```

### E. La Gran Batalla: DIV vs SPAN
Los contenedores "genéricos". No significan nada, solo agrupan.
*   **DIV (Division):** Es de **BLOQUE**. Sirve para agrupar secciones grandes.
*   **SPAN:** Es de **LÍNEA**. Sirve para dar estilo a una palabrita dentro de un párrafo.

> 🎓 **Abre este archivo:** `codigos/03_07_div_span.html`
> Es crucial que entiendas la diferencia visual entre estos dos.

```html
<!-- DIV es BLOQUE (Salto de línea automático) -->
<div style="background: red;">Soy una caja grande</div>
<div style="background: blue;">Soy otra caja abajo</div>

<!-- SPAN es LÍNEA (Se queda al lado) -->
<p>
    Texto normal con una <span style="color: red;">palabra roja</span> en medio.
</p>
```

## 4. Atributos Globales (Los Superpoderes)

Existen atributos que le puedes poner a **CUALQUIER** etiqueta HTML.

| Atributo | Uso | Ejemplo |
| :--- | :--- | :--- |
| `class` | **El más usado.** Asigna un nombre de "clase" para darle estilos CSS a varios elementos a la vez. | `<p class="alerta">Error</p>` |
| `id` | **Identificador Único.** Como una CURP/DNI. Solo puede haber UN elemento con ese ID en toda la página. Se usa para JS. | `<div id="menu-principal">` |
| `title` | Muestra un "tooltip" (globo de texto) al dejar el mouse encima. Bueno para accesibilidad. | `<button title="Guardar cambios">💾</button>` |
| `style` | Aplica CSS directo (Inline). Útil para pruebas rápidas, malo para producción. | `<p style="color:red;">` |
| `hidden` | Oculta el elemento completamente (no se ve, pero existe). | `<p hidden>Secreto</p>` |

---

## 5. El Atributo `alt` (Ojo con esto)

En las imágenes `<img>`, el atributo `alt` NO es opcional en el mundo profesional.
*   **Propósito:** Describe la imagen en texto.
*   **¿Para quién?**
    1.  Personas ciegas (su lector de pantalla les lee "Foto de un perro corriendo" en lugar de "img_402.jpg").
    2.  Si tu internet falla y la imagen no carga, aparecerá el texto.
    3.  Google usa ese texto para saber qué hay en la foto.

---

## 6. Práctica Sugerida

Intenta crear esto en un archivo nuevo `practica_bloques.html`:

1.  Crea dos párrafos (`p`). Fíjate cómo se ponen uno debajo del otro (Bloque).
2.  Dentro del segundo párrafo, pon tres enlaces (`a`) a Google, Facebook y Twitter. Fíjate cómo se ponen uno al lado del otro (Línea).
3.  Ponle un `title` a uno de los enlaces para ver el tooltip.

> 📂 **Códigos de Ejemplo:**
> *   `codigos/03_01_bloque_vs_linea.html` (Demo visual con bordes rojos/azules)
> *   `codigos/03_02_atributos.html` (Tooltips, IDs y Clases)
> *   `codigos/03_03_listas_desordenadas.html` (Bullets `ul`)
> *   `codigos/03_04_listas_ordenadas.html` (Números `ol`)
> *   `codigos/03_05_listas_definicion.html` (Diccionarios `dl`)
> *   `codigos/03_06_tablas.html` (Filas y Celdas)
> *   `codigos/03_07_div_span.html` (La gran diferencia visual)

<div align="center">

⬅️ [Anterior: 0.2 Basic](0.2_html_basic.md) &nbsp;&nbsp;|&nbsp;&nbsp; [Siguiente: 0.4 Headings](0.4_html_headings_paragraphs.md) ➡️

</div>
