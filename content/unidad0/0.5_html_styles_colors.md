# 0.5 HTML Styles & Colors: El Maquillaje de la Web

> 🎨 **Objetivo de la Lección:** Entender cómo se aplica diseño a HTML, aprender el atributo `style`, conocer los sistemas de colores (Nombres, HEX, RGB) y la propiedad `font-family`.

---

## 1. El Conflicto: HTML vs CSS

Originalmente, HTML fue creado solo para **información** (texto plano).
Si querías poner algo en rojo, tenías que inventar una etiqueta `<font color="red">`. Esto se volvió un caos.

Para resolverlo, nació **CSS (Cascading Style Sheets)**.
*   **HTML:** ESTRUCTURA (¿Qué es esto? Un título).
*   **CSS:** PRESENTACIÓN (¿Cómo se ve? Rojo y Grande).

En el mundo real, usamos archivos `.css` separados. Pero para aprender, usaremos el **atributo `style`** (Inline CSS).

---

## 2. El Atributo `style`

Sintaxis Universal:
```html
<etiqueta style="propiedad: valor; propiedad: valor;">
```
*   **Propiedad:** ¿Qué quiero cambiar? (Color, Fondo, Tamaño).
*   **Valor:** ¿Cómo lo quiero? (Rojo, Azul, 20px).
*   **Importante:** Se separan con punto y coma `;`.

---

## 3. Propiedades CSS Esenciales

### A. Color de Fondo (`background-color`)
Define el color de la "caja" del elemento.
```html
<body style="background-color: powderblue;"> <!-- Fondo de toda la página -->
<h1 style="background-color: yellow;"> <!-- Fondo solo del título (marcatextos) -->
```

### B. Color de Texto (`color`)
Define el color de la letra.
```html
<p style="color: blue;">Soy azul</p>
<p style="color: red;">Soy rojo</p>
```

### C. Fuentes (`font-family`)
Cambia la tipografía. Las fuentes se agrupan en familias.
*   `serif`: Con "patines" (Times New Roman). Elegante.
*   `sans-serif`: Sin patines (Arial, Verdana, Helvetica). Moderna y limpia.
*   `monospace`: Todos los caracteres tienen el mismo ancho (Courier). Para código.

```html
<h1 style="font-family: Verdana;">Título Moderno</h1>
<p style="font-family: Courier;">Parece una máquina de escribir.</p>
```

### D. Tamaño de Fuente (`font-size`)
Se puede medir en pixeles (`px`), porcentaje (`%`) o unidades relativas (`em`, `rem`).
```html
<h1 style="font-size: 300%;">GIGANTE</h1>
<p style="font-size: 16px;">Tamaño estándar de lectura.</p>
```

### E. Alineación (`text-align`)
*   `left` (Izquierda - Default).
*   `center` (Centrado).
*   `right` (Derecha).
*   `justify` (Justificado - Como libro).

```html
<h1 style="text-align: center;">Título Centrado</h1>
```

---

## 4. Teoría del Color: Tres Formas de Pintar 🌈

HTML soporta 140 nombres de colores estándar, pero millones de combinaciones matemáticas.

### 1. Nombres (HTML Color Names)
Son palabras en inglés. Hay 140 disponibles.
*   `red`, `blue`, `green`, `orange`, `purple`, `black`, `white`.
*   Colores curiosos: `Tomato`, `DodgerBlue`, `MediumSeaGreen`, `SlateBlue`, `Violet`, `LightGray`.

### 2. Hexadecimal (HEX) - *El Estándar Pro*
Usa un gato `#` seguido de 6 dígitos (0-9 y A-F).
*   Los primeros 2 son Rojo (RR).
*   Los segundos 2 son Verde (GG).
*   Los últimos 2 son Azul (BB).
*   *Ejemplos:*
    *   `#000000` = Negro Absoluto (0 luz).
    *   `#FFFFFF` = Blanco Absoluto (Toda la luz).
    *   `#FF0000` = Rojo Puro.

### 3. RGB (Red, Green, Blue)
Define la intensidad de 0 a 255.
*   `rgb(255, 0, 0)` = Rojo.
*   `rgb(60, 60, 60)` = Gris oscuro.
*   **Bonus:** `rgba(255, 0, 0, 0.5)` = Rojo con 50% de **transparencia** (Alpha).

---

## 5. Reto: El Modo Oscuro (Dark Mode)

Intenta escribir el código para una página que tenga:
1.  Fondo general color Negro (o un gris muy oscuro `#333`).
2.  Texto principal en blanco o gris claro (`lightgray`).
3.  Un encabezado centrado en color `gold` (Dorado).
4.  Un párrafo con fuente `Verdana`.

> 📂 **Códigos de Ejemplo:**
> *   `codigos/05_01_colores.html` (Hex vs RGB vs Nombres)
> *   `codigos/05_02_fuentes.html` (Familias tipográficas y alineación)
> *   `codigos/05_03_reto_modo_oscuro.html` (SOLUCIÓN al reto)
> *   `codigos/05_04_bordes.html` (Bordes, Redondeados y Círculos)
> 🎓 **Abre este archivo:** `codigos/05_04_bordes.html`

```html
<!-- Borde punteado y rojo -->
<div style="border: 3px dotted red;">¡Peligro!</div>

<!-- Círculo perfecto (Border Radius 50%) -->
<div style="width: 100px; height: 100px; border-radius: 50%; background: blue;"></div>
```
> Si no entiendes la diferencia entre padding y margin, sufrirás diseño web por años. Entra y apréndelo ya.

```html
<style>
    .caja {
        border: 2px solid black;
        /* Relleno INTERNO (Le da aire al texto) */
        padding: 20px;
        /* Espacio EXTERNO (Empuja a los vecinos) */
        margin: 50px;
    }
</style>
> 🎓 **Abre estos archivos:**
> *   `codigos/05_06_fondo_imagen.html`
> *   `codigos/05_07_opacidad.html`

```html
<!-- Imagen de fondo (CSS) -->
<body style="background-image: url('fondo.jpg'); background-repeat: no-repeat;">
    
<!-- Fantasma (Opacidad 50%) -->
<div style="opacity: 0.5; background: red;">Soy semitransparente</div>
``` (Transparencias)

<div align="center">

⬅️ [Anterior: 0.4 Headings](0.4_html_headings_paragraphs.md) &nbsp;&nbsp;|&nbsp;&nbsp; [Siguiente: 0.6 Multimedia](0.6_html_images_links.md) ➡️

</div>
